import React from 'react';
import ProductCategory from '../components/ProductCategory';

const Watches = (props) => {
  return <ProductCategory title="WATCHES" category="Watches" {...props} />;
};

export default Watches;
