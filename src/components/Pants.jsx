import React from 'react';
import ProductCategory from '../components/ProductCategory';

const Pants = (props) => {
  return <ProductCategory title="PANTS" category="Pants" {...props} />;
};

export default Pants;
